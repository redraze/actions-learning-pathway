name: hello-world
on: push
jobs:
  hello-world-job:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: checkout/@V4
      - run: echo "$(cat hello_world.txt)"
